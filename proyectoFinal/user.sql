use proyecto_final;

-- insert into user (email, password, role)
-- values ('maria@host.com', 'mypass4', 'user')

 -- insert into user (email, password, role)
 -- values ('pepe@host.com', 'mypass3', 'user')

SELECT * FROM user;


delete from user where id = 6;